ext {
    androidxLifecycleVersion = "2.3.1"

    // hilt
    hiltVersion = "2.38.1"

    // rx
    rxJavaVersion = "2.2.21"
    rxAndroidVersion = "2.1.1"

    // retrofit & okhttp
    retrofitVersion = "2.9.0"
    okHttpVersion = "4.9.1"

    // serialization
    kotlinSerializationVersion = "1.3.0"
    kotlinxSerializationConverterVersion = "0.8.0"

    // glide
    glideVersion = "4.12.0"

    // indicator
    indicatorVersion = "2.1.6"
}